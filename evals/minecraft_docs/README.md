# minecraft_docs development evaluation

`development.json` is a compact deterministic fixture for regression checks. Each record declares expected corpora, optional required symbols, and negative expectations. The held-out expansion should remain separate from tuning data.
